<####>
<# TEMPLATE DATA #>
<# DATA:RENDERABLE [ renderable = CustomRenderable() ] #>
<####>

@props(['for'])

@error($for)
    <p {{ $attributes->merge(['class' => 'vtheme(ui.input.error)']) }}>{{ $message }}</p>
@enderror
